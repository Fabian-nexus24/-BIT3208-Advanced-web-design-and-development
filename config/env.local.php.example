<?php
declare(strict_types=1);

/**
 * Local / production overrides — copy to env.local.php (not committed).
 * env.local.php is loaded by config/db.php and config/app.php when present.
 */

return [
    'APP_ENV'   => 'local',
    'BASE_URL'  => 'http://localhost/farmconnect/',
    // When true (default), BASE_URL is detected from the browser URL — required for ngrok/mobile testing.
    'AUTO_BASE_URL' => true,
    'DB_HOST'   => 'localhost',
    'DB_NAME'   => 'farmconnect_kenya',
    'DB_USER'   => 'root',
    'DB_PASS'   => '',
];
